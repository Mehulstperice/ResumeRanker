<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Resume Match Result - ResumeRanker</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f0f2f5;
       padding-top: 0px;
       background-color: #f0f2f5;

      }

      .result-card {
        background-color: #ffffff;
        padding: 2rem;
        border-radius: 1rem;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        width: 100%;
        margin: auto;
      }

      .badge-score {
        font-size: 1.1rem;
        background-color: #6c63ff;
        padding: 0.5rem 1rem;
      }

      .btn-download {
        background-color: #6c63ff;
        border-color: #6c63ff;
        margin-top: 20px;
      }

      .missing-list {
        margin-top: 1rem;
      }

      .missing-list span {
        margin-right: 8px;
        margin-bottom: 8px;
        display: inline-block;
        background: #e0e0e0;
        padding: 5px 10px;
        border-radius: 20px;
        font-size: 0.9rem;
      }
    </style>
  </head>
  <body>
    <%- include("partials/navbar") %>

    <div class="result-card text-center">
      <h2 class="mb-3">Match Result for <%= resume.filename %></h2>

      <p>
        <strong>Match Score:</strong>
        <span class="badge rounded-pill text-white badge-score"><%= resume.score %>%</span>
      </p>

      <div class="missing-list">
        <h5 class="mt-4">Missing Skills</h5>
        <% if (resume.missingSkills.length > 0) { %>
          <% resume.missingSkills.forEach(skill => { %>
            <span><%= skill %></span>
          <% }) %>
        <% } else { %>
          <p class="text-success mt-2">None! You matched all the keywords 🎉</p>
        <% } %>
      </div>

      <a
        href="/resume/download/<%= resume._id %>"
        class="btn btn-primary btn-download"
        download
      >
        Download PDF Report
      </a>
    </div>
  </body>
</html>
